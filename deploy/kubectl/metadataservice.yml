apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-service
spec:
  selector:
    matchLabels:
      app: metadata-service
  template:
    metadata:
      labels:
        app: metadata-service
    spec:
      containers:
        - name: server
          image: registry.gitlab.com/arjit95/vidstream/metadata-service:v1
          ports:
          - containerPort: 8080
          readinessProbe:
            initialDelaySeconds: 10
            httpGet:
              path: "/_healthz"
              port: 8080
          livenessProbe:
            initialDelaySeconds: 10
            httpGet:
              path: "/_healthz"
              port: 8080
          envFrom:
          - configMapRef:
              name: network-config
          - configMapRef:
              name: storage-config
          - configMapRef:
              name: queue-config
          - secretRef:
              name: network-secret
          env:
          - name: PORT
            value: "8080"
          volumeMounts:
          - name: pv0001
            mountPath: /data

      volumes:
      - name: pv0001
        persistentVolumeClaim:
          claimName: upload-pvc
          readOnly: false

---
apiVersion: v1
kind: Service
metadata:
  name: metadata-service
spec:
  type: ClusterIP
  selector:
    app: metadata-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080