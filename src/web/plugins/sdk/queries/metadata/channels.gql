query getUserChannels($username: String!, $limit: Int = 10, $skip: Int = 0 ) {
  channels(username: $username, limit: $limit, skip: $skip) {
    total
    result {
      id
      title
      subscribers
    }
  }
}
